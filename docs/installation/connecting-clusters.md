# Connecting clusters

In **Decoupled** mode, you must connect your workload clusters to your storage clusters. This allows the hypervisors to consume the storage backend provided by the storage cluster.

*Coming soon: detailed runbook for connecting workload clusters to storage backends.*

## Related

- [Configure a cluster](configuring-a-cluster.md): define each AZ with a `Cluster` resource.
- [Deployment topology](../architecture/deployment-topology.md): when to use decoupled storage and workload.
